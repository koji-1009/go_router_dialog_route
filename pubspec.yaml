name: go_router_dialog_route
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

  riverpod_annotation: ^2.0.0
  flutter_riverpod: ^2.0.0
  go_router: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

  build_runner: ^2.0.0
  riverpod_generator: ^2.0.0
  go_router_builder: ^2.0.0

flutter:
  uses-material-design: true
